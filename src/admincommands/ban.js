//Test remote commit
//Test remote commit2
class Ban {
    constructor(member, reason, modRight){
        this.member = member; 
        this.reason = reason; 
        this.modRight = modRight; //modRight is a boolean value on whether moderators are allowed to ban players
    }

    banMember(){
        if()
    }
}
